using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SquareManagementScript : MonoBehaviour
{
    public bool squareUIisActive = false;
    public bool allowSquareUIActivation = true;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
